//if (active_style != 2) || (active_style == 2 && activate == 1)
//{
	currentSpeed = platformSpeed;//}

if (active_style == 3)
{
	if (goingToStart == false)
	{goingToStart = true}
	else
	{goingToStart = false}
}